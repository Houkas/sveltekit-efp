export type Realisation = {
    attributes: {
        title: string;
        meta_description: string;
        slug: string;
        miniature: string;
        images: string;
        body: string;
    }
}