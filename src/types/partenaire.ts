export type Partenaire = {
    attibutes: {
        nom: string;
        logo: {
            data: {
                attributes: {
                    url: string;
                }
            }
        };
    }
}